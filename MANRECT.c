#include"stdio.h"
int main()
{
  int t,xl,xu,yl,yu,q=0,dist=0,x0=0,y0=0,distance[100];
  scanf("%d",&t);
  while(t--)
  {
    while(dist>0)
    {
      printf("\nQ %d %d",x0,y0);
      scanf("%d",&dist);
      if(dist<0)
      {
        break;
        //x0+=2,y0+=3;
      }
     // if(!x0&&!y0)

    }
    //printf("\nA %d %d %d %d", );
  }
}